model User {
  id         String       @id @default(uuid())
  email      String       @unique
  password   String
  name       String
  likes      Tweet[]      @relation("TweetLikes")
  tweets     Tweet[]
  comments   Comment[]
  // Following  User[]
  // Followers  User[]
}